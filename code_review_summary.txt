The changes in App.axaml are correct and address the issue:
- RequestedThemeVariant="Dark" is essential for the dark UI to ensure theme-dependent elements (like ComboBox popups and TextBox focus templates) use dark-compatible colors.
- The global styles for TextBox:focus and TextBox:pointerover target the Avalonia template's PART_BorderElement, which is exactly what was causing the visual override.
- Building the project confirmed that the XAML is valid.
